<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detail Bunga</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1>Detail Bunga</h1>
  <img src="/uploads/<%= bunga.gambar %>" alt="<%= bunga.nama_bunga %>">
  <h2><%= bunga.nama_bunga %></h2>
  <p>Harga: Rp<%= bunga.harga %></p>
  <p>Deskripsi: <%= bunga.deskripsi %></p>
  <form action="/pelanggan/pesanan" method="POST">
    <input type="hidden" name="kd_bunga" value="<%= bunga.kd_bunga %>">
    <input type="hidden" name="id_pelanggan" value="/* ID Pelanggan */">
    <input type="hidden" name="tgl_pesan" value="<%= new Date().toISOString().split('T')[0] %>">
    <button type="submit">Pesan Sekarang</button>
  </form>
</body>
</html>
