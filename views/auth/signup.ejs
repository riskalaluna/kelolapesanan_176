<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registrasi Pelanggan</title>
  <style>
    /* Mengatur font dasar dan latar belakang */
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f9f9f9; /* Warna pastel abu-abu muda */
      margin: 0;
      padding: 0;
    }

    /* Mengatur tata letak form registrasi */
    .registration-container {
      width: 100%;
      max-width: 500px;
      margin: 50px auto;
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    /* Judul halaman */
    h1 {
      color: #5c5c5c;
      font-size: 28px;
      margin-bottom: 20px;
    }

    /* Label input */
    label {
      display: block;
      text-align: left;
      margin-bottom: 5px;
      font-weight: bold;
    }

    /* Input fields */
    input[type="text"],
    input[type="password"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 5px;
      box-sizing: border-box;
      font-size: 14px;
    }

    input[type="text"]:focus,
    input[type="password"]:focus {
      border-color: #ff6f61; /* Warna pastel coral */
      outline: none;
    }

    /* Tombol Daftar */
    button {
      background-color: #ff6f61; /* Warna pastel coral */
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 5px;
      width: 100%;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: #ff4040; /* Warna lebih gelap saat hover */
    }

    /* Tautan login */
    p {
      font-size: 14px;
    }

    a {
      color: #ff6f61; /* Warna pastel coral */
      text-decoration: none;
      font-weight: bold;
    }

    a:hover {
      color: #ff4040; /* Warna lebih gelap saat hover */
    }
  </style>
</head>
<body>

  <!-- Container registrasi -->
  <div class="registration-container">
    <h1>Registrasi Pelanggan</h1>

    <!-- Form registrasi -->
    <form action="/pelanggan/signup" method="POST">
      <label for="nam_pelanggan">Nama:</label>
      <input type="text" name="nam_pelanggan" placeholder="Nama" required>

      <label for="alamat_pelanggan">Alamat:</label>
      <input type="text" name="alamat_pelanggan" placeholder="Alamat" required>

      <label for="usn_pelanggan">Username:</label>
      <input type="text" name="usn_pelanggan" placeholder="Username" required>

      <label for="pw_pelanggan">Password:</label>
      <input type="password" name="pw_pelanggan" placeholder="Password" required>

      <button type="submit">Daftar</button>
    </form>

    <!-- Tautan untuk login -->
    <p>Sudah punya akun? <a href="/auth/login">Login di sini</a></p>
  </div>

</body>
</html>
