<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Toko Bunga</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f7f7f7;
            color: #333;
        }
        h1 {
            text-align: center;
            color: linear-gradient(135deg, rgba(142, 45, 226, 2.4), rgba(46, 6, 127, 2.4)); /* Opacity 80% */
            margin-top: 50px;
            font-size: 2.5em;
        }
        form {
            max-width: 400px;
            margin: 30px auto;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1em;
            background-color: #f0f0f0;
        }
        input:focus {
            outline: none;
            border-color: #FFB6C1;
        }
        button {
            width: 100%;
            background: linear-gradient(135deg, rgba(142, 45, 226, 2.4), rgba(46, 6, 127, 2.4)); /* Opacity 80% */
            border: none;
            color: white;
            padding: 12px;
            font-size: 1.2em;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }
        button:hover {
            background-color: #66BB6A;
            transform: translateY(-3px);
        }
        p {
            text-align: center;
            margin-top: 20px;
        }
        a {
            color: linear-gradient(135deg, rgba(142, 45, 226, 2.4), rgba(46, 6, 127, 2.4)); /* Opacity 80% */
            text-decoration: none;
            font-weight: bold;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Login</h1>
    <form action="/auth/login" method="POST">
        <input type="text" name="usn" placeholder="Username" required>
        <input type="password" name="pw" placeholder="Password" required>
        <button type="submit">Login</button>
    </form>
    <p>Belum punya akun? <a href="/auth/signup">Signup disini</a></p>
</body>
</html>
